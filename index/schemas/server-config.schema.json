{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://open77.dev/schemas/server-v1.json",
  "title": "Open77 dedicated server configuration",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "identity", "network", "simulation", "masterServer"],
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "const": 1 },
    "identity": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "description", "locale", "tags", "visibility"],
      "properties": {
        "name": { "type": "string", "minLength": 1, "maxLength": 96 },
        "description": { "type": "string", "maxLength": 512 },
        "locale": {
          "type": "string",
          "pattern": "^[A-Za-z]{2,3}(?:-[A-Za-z0-9]{2,8})*$"
        },
        "tags": {
          "type": "array",
          "maxItems": 16,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9_-]{0,23}$"
          }
        },
        "website": {
          "type": ["string", "null"],
          "format": "uri",
          "maxLength": 2048
        },
        "discord": {
          "type": ["string", "null"],
          "format": "uri",
          "maxLength": 2048
        },
        "icon": {
          "type": ["string", "null"],
          "description": "Relative PNG path, square, at most 256 KiB. Warden enforces exactly the bounds below on upload (Branding/BrandingConstraints.cs); the loader below is looser, which is the trap. USE 64-512 px per side: this server accepts 32-512 (ServerIconLoader) but the website's display guard rejects anything under 64 on the shorter side (app/src/lib/server-images.ts), so only the overlap actually renders and a 32-48 px icon validates here then silently shows a placeholder on the site. That guard also rejects a longer/shorter skew above 1.2, so keep it exactly square. 256x256 recommended."
        },
        "banner": {
          "type": ["string", "null"],
          "description": "Relative PNG/JPEG/WebP path for the wide hero image; width 320-4096 px, aspect 2.0-5.0 and at most 2 MiB. 1200x400 (3:1) is the recommended size. BUDGET, and it binds far tighter than the 2 MiB: the icon and the banner travel base64-encoded inside ONE registration request, and the master caps a request body at 512 KiB (master/src/Open77.Master/Program.cs), so the two files together must stay under ~378 KiB of raw bytes. Over that, the registration is refused with a bare HTTP 413 and the server does not appear in the catalogue AT ALL -- a worse failure than having no branding. Warden checks the pair before writing; a hand-placed pair is flagged at startup. See Branding/BrandingConstraints.cs."
        },
        "visibility": {
          "type": "string",
          "enum": ["public", "unlisted", "private"]
        }
      }
    },
    "network": {
      "type": "object",
      "additionalProperties": false,
      "required": ["port", "publicEndpoint", "maximumPlayers"],
      "properties": {
        "port": { "type": "integer", "minimum": 1, "maximum": 65535 },
        "publicEndpoint": {
          "type": ["string", "null"],
          "description": "Public hostname or IP with port, e.g. play.example.net:11778."
        },
        "maximumPlayers": { "type": "integer", "minimum": 1, "maximum": 1024 }
      }
    },
    "simulation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["tickRate", "snapshotRate", "expectedGameBuild", "helloTimeoutSeconds"],
      "properties": {
        "tickRate": { "type": "integer", "minimum": 1, "maximum": 240 },
        "snapshotRate": { "type": "integer", "minimum": 1, "maximum": 240 },
        "expectedGameBuild": { "type": "integer", "minimum": 1 },
        "helloTimeoutSeconds": { "type": "integer", "minimum": 1, "maximum": 60 },
        "playerInterestRadius": { "type": "number", "minimum": 0, "maximum": 100000 },
        "playerInterestHysteresis": { "type": "number", "minimum": 0, "maximum": 10000 }
      }
    },
    "voice": {
      "type": "object",
      "additionalProperties": false,
      "description": "Server-authoritative integrated Opus voice policy.",
      "properties": {
        "enabled": { "type": "boolean" },
        "quality": { "type": "string", "enum": ["low", "standard", "high", "ultra"] },
        "proximityDistance": { "type": "number", "minimum": 0, "maximum": 1000 },
        "minimumClientProximityDistance": { "type": "number", "minimum": 0, "maximum": 1000 },
        "maximumClientProximityDistance": { "type": "number", "minimum": 0, "maximum": 1000 },
        "maximumChannelAudience": { "type": "integer", "minimum": 1, "maximum": 1024 },
        "maximumSimultaneousTalkers": { "type": "integer", "minimum": 1, "maximum": 1024 },
        "maximumTalkersPerRecipient": { "type": "integer", "minimum": 1, "maximum": 64 },
        "globalEgressKbps": { "type": "integer", "minimum": 0, "maximum": 1000000 },
        "recipientEgressKbps": { "type": "integer", "minimum": 0, "maximum": 100000 },
        "ingressRejectLimit": { "type": "integer", "minimum": 20, "maximum": 100000 },
        "ingressRejectWindowSeconds": { "type": "integer", "minimum": 1, "maximum": 300 },
        "ingressBackoffMilliseconds": { "type": "integer", "minimum": 50, "maximum": 10000 }
      }
    },
    "resources": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": { "type": "boolean" },
        "root": { "type": "string", "minLength": 1 },
        "load": {
          "type": "array",
          "description": "Ordered resource directory rules. Entries add matches; a leading ! removes them. Paths are relative to resources.root unless absolute and support *, ? and **.",
          "maxItems": 256,
          "items": { "type": "string", "minLength": 1, "maxLength": 2048 }
        },
        "autoStart": { "type": "boolean" },
        "watchIntervalMilliseconds": { "type": "integer", "minimum": 0, "maximum": 60000 },
        "download": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "enabled": { "type": "boolean" },
            "listenUrl": { "type": "string", "format": "uri" },
            "publicBaseUrl": { "type": "string", "format": "uri" },
            "cacheDirectory": { "type": "string", "minLength": 1 },
            "signingKeyFile": { "type": "string", "minLength": 1 },
            "chunkSizeBytes": { "type": "integer", "minimum": 65536, "maximum": 4194304 }
          }
        }
      }
    },
    "requiredMods": {
      "type": "object",
      "additionalProperties": false,
      "description": "The mods every player must load to join this world. Absent or empty means 'requires nothing', which is what every server means until an owner opts in.",
      "properties": {
        "enabled": { "type": "boolean" },
        "unsecured": {
          "type": "boolean",
          "description": "UNSECURED MODE. Lets this server require executable packages that carry no verified attestation (and lets a resource preload them). Every server list shows the server with an 'unsecured' flag and players must confirm the risk before joining. Off by default."
        },
        "note": { "type": "string", "maxLength": 512 },
        "packages": {
          "type": "array",
          "maxItems": 64,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "description": "Two custody models. An 'indexed' package names an entry in the platform's signed index and may carry only minVersion; a 'hosted' package is served by this server and must pin version, sha256 and sizeBytes.",
            "required": ["id"],
            "properties": {
              "id": { "type": "string", "pattern": "^[a-z0-9][a-z0-9_-]{0,63}$" },
              "source": { "enum": ["indexed", "hosted"] },
              "displayName": { "type": "string", "maxLength": 96 },
              "minVersion": { "type": "string", "pattern": "^[0-9]+(\\.[0-9]+){0,3}(-[0-9A-Za-z.-]{1,32})?$" },
              "version": { "type": "string", "pattern": "^[0-9]+(\\.[0-9]+){0,3}(-[0-9A-Za-z.-]{1,32})?$" },
              "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
              "sizeBytes": { "type": "integer", "minimum": 0 }
            }
          }
        }
      }
    },
    "accessControl": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "file": {
          "type": "string",
          "minLength": 1,
          "description": "Relative JSONC file containing authenticated public-key ACL principals."
        }
      }
    },
    "database": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": { "type": "boolean" },
          "connectionString": {
            "type": "string",
            "description": "MySQL connection string used by resources with the database.access permission."
          },
          "connectionStringEnvironmentVariable": {
            "type": "string",
            "minLength": 1,
            "default": "OP77_DATABASE_CONNECTION",
            "description": "Environment variable holding the MySqlConnector connection string. A nonblank explicit connectionString takes precedence. Read once at server startup."
          },
        "maxRows": { "type": "integer", "minimum": 1, "maximum": 1000000 }
      }
    },
    "http": {
      "type": "object",
      "additionalProperties": false,
      "description": "Outbound HTTP for resources holding the http.request permission (PerformHttpRequest). Off by default; nothing is reachable until allowedHosts names a destination. Redirects are never followed.",
      "properties": {
        "enabled": { "type": "boolean" },
        "allowedHosts": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Exact hosts (discord.com) or one level of subdomain (*.example.com). No scheme, path or port."
        },
        "timeoutSeconds": { "type": "integer", "minimum": 1, "maximum": 60 },
        "maxResponseBytes": { "type": "integer", "minimum": 1024, "maximum": 16777216 }
      }
    },
    "httpHandlers": {
      "type": "object",
      "additionalProperties": false,
      "description": "Inbound HTTP for resources holding the http.serve permission (Open77.http.listen / SetHttpHandler). Off by default and bound to loopback by default; plain HTTP with no authentication, so anything reachable from the internet goes behind a reverse proxy. Routes are /<resource>/<prefix>.",
      "properties": {
        "enabled": { "type": "boolean" },
        "listenUrl": {
          "type": "string",
          "format": "uri",
          "description": "Absolute http:// origin the listener binds, e.g. http://127.0.0.1:11781. No https, no path."
        },
        "timeoutSeconds": {
          "type": "integer", "minimum": 1, "maximum": 60,
          "description": "How long a request waits for its Lua handler before the server answers 504 handler_timeout on its behalf."
        }
      }
    },
    "warden": {
      "type": "object",
      "additionalProperties": false,
      "description": "The Warden in-process admin panel. Off by default.",
      "properties": {
        "enabled": { "type": "boolean" },
        "hubFileGatewayOrigin": {
          "type": ["string", "null"],
          "format": "uri",
          "description": "Trusted Hub file gateway origin. HTTPS, or HTTP with an explicit loopback IP for local testing. No path, credentials, query or fragment. Omit to disable Hub installation."
        },
        "listenUrl": {
          "type": "string",
          "format": "uri",
          "description": "Absolute HTTP URL the admin panel binds, e.g. http://0.0.0.0:11780."
        }
      }
    },
    "convars": {
      "type": "object",
      "description": "FiveM-style convars: a flat name/value bag of strings read from Lua by GetConvar, GetConvarInt and GetConvarBool. This is a COMPATIBILITY SHIM so a resource ported from FiveM runs without its configuration layer being rewritten; it is not the mechanism to reach for when writing something new. Open77.tunables is better on every axis that matters -- a tunable is declared with a type and a range, validated on every write, retunable by the operator from the Warden panel while the server runs, persisted across a restart, and scoped to the resource that declared it, which also receives onTunableChanged. A convar is an untyped string, server-wide, and a SetConvar from Lua is in-memory only and gone on the next restart. Keep it credential-free -- this file is tracked, so a token or password belongs in the environment like every other credential in this schema.",
      "propertyNames": { "pattern": "^[A-Za-z0-9_.:-]{1,128}$" },
      "additionalProperties": { "type": "string", "maxLength": 4096 }
    },
    "startup": {
      "type": "object",
      "additionalProperties": false,
      "description": "What this server does to itself once it has finished starting.",
      "properties": {
        "commands": {
          "type": "array",
          "maxItems": 64,
          "description": "Console commands run once, in order, after resources have started -- the server-side equivalent of exec in a server.cfg. Each line goes through the same dispatcher as a line typed at the stdin console and carries exactly that authority, never a player's. Per server by design: this is how a gamemode's host pins world state (for example Pursuit's fixed clock and weather) without editing a resource every other server also loads. A line the dispatcher refuses is logged as an error and the server starts anyway. Keep it credential-free -- this file is tracked, so a token or password belongs in the environment.",
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 512,
            "pattern": "^[^\\u0000-\\u001F\\u007F]+$"
          }
        }
      }
    },
    "masterServer": {
      "type": "object",
      "additionalProperties": false,
      "required": ["enabled"],
      "properties": {
        "enabled": { "type": "boolean" },
        "serverId": {
          "type": ["string", "null"],
          "format": "uuid",
          "description": "Stable server identity issued or approved by the Master."
        },
        "url": {
          "type": ["string", "null"],
          "format": "uri"
        },
        "tokenEnvironmentVariable": {
          "type": "string",
          "pattern": "^[A-Za-z_][A-Za-z0-9_]{0,63}$"
        },
        "identityFile": {
          "type": "string",
          "minLength": 1,
          "description": "Relative file used for the automatically generated Master identity."
        },
        "heartbeatIntervalSeconds": {
          "type": "integer",
          "minimum": 10,
          "maximum": 300
        },
        "licenseKey": {
          "type": ["string", "null"],
          "pattern": "^op77_live_[A-Za-z0-9_-]{43}$",
          "description": "Platform license key created in the keymaster. Prefer the environment variable so this file stays credential-free."
        },
        "licenseKeyEnvironmentVariable": {
          "type": "string",
          "pattern": "^[A-Za-z_][A-Za-z0-9_]{0,63}$",
          "description": "Environment variable consulted when licenseKey is not set (default OP77_LICENSE_KEY)."
        },
        "requireConnectTicket": {
          "type": "boolean",
          "description": "Require a valid master-signed connect ticket in ClientHello to join."
        },
        "publishPlayerList": {
          "type": "boolean",
          "description": "Publish the display names of the players currently online with the heartbeat, so the server's public page shows who is on and not just how many. Default true: those names are already visible to everyone in the world and joining is free. Nothing else is published - no account id, no address, no position. Set false and the directory answers players: null, which is distinct from an empty roster."
        },
        "devLocalAuth": {
          "type": "boolean",
          "description": "Local development bypass. When true the server accepts unauthenticated clients (no master certificate, no connect ticket) and binds the game socket to loopback only. Accepted ONLY when enabled=false, visibility=private and requireConnectTicket=false; never usable on a public/mastered server. For agent/MCP local dev without the prod launcher."
        }
      }
    }
  }
}
