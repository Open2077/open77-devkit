# Samples

Resources written by a headless agent (`claude -p` with `--strict-mcp-config`, nothing but the
Devkit MCP attached, offline index) from the prompts in `../tasks.json`, kept as they were
produced. Each passes `run-static.mjs` and the server's own `--lint` for the build in
`../tasks.json`. They are evidence of what the MCP makes possible, not reviewed resources.
